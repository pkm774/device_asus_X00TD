allow bluetooth hal_perf_default:binder { call };
allow bluetooth system_data_file:file { open };
allow bluetooth vendor_iop_prop:file { read getattr map open };
allow bluetooth vendor_default_prop:file { read getattr map open };

allow bluetooth property_socket:sock_file { write };
allow bluetooth init:unix_stream_socket { connectto };
allow bluetooth bluetooth_prop:property_service { set };
allow bluetooth bluetooth_prop:file { read getattr map open };
